// schema.prisma
datasource db {
  provider = "postgresql"
  url      = "postgresql://postgres:postgres@localhost:4002/userdb"
}

generator client {
  provider = "prisma-client-js"
}

model User {
  id     Int    @id @default(autoincrement())
  name   String
  email  String @unique
  number String
}
